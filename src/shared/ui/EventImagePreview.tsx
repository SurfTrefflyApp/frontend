import { Title } from "@/shared/icons/Title";

export const EventImagePreview = () => {
  return (
    <div className="w-full h-full bg-surface-container-highest flex justify-center items-center rounded-lg aspect-video">
      <Title className="text-secondary" />
    </div>
  );
};
